stages:
  - package
  - publish

variables:
  PACKAGE_REGISTRY_URL: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic"

# Discover skill directories (any dir containing SKILL.md)
.skill_dirs: &skill_dirs
  - |
    SKILLS=$(find . -maxdepth 1 -mindepth 1 -type d \
      ! -name '.*' \
      -exec test -f '{}/SKILL.md' \; -print \
      | sed 's|^\./||' | sort)

package:
  stage: package
  image: alpine:latest
  before_script:
    - apk add --no-cache zip findutils
  script:
    - *skill_dirs
    - mkdir -p dist
    - |
      for skill in $SKILLS; do
        echo "Packaging: $skill"
        (cd "$skill" && zip -r "../dist/${skill}.skill" . -x '.*')
        echo "  -> dist/${skill}.skill ($(du -h "dist/${skill}.skill" | cut -f1))"
      done
    - echo "--- Packaged skills ---"
    - ls -lh dist/
  artifacts:
    paths:
      - dist/*.skill
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

publish:
  stage: publish
  image: alpine:latest
  needs:
    - package
  before_script:
    - apk add --no-cache curl findutils
  script:
    - *skill_dirs
    - VERSION="${CI_COMMIT_TAG:-${CI_COMMIT_SHORT_SHA}}"
    - |
      for skill in $SKILLS; do
        FILE="dist/${skill}.skill"
        if [ ! -f "$FILE" ]; then
          echo "WARN: $FILE not found, skipping"
          continue
        fi

        echo "Publishing: $skill @ $VERSION"
        curl --fail --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
          --upload-file "$FILE" \
          "${PACKAGE_REGISTRY_URL}/${skill}/${VERSION}/${skill}.skill"

        # Also publish as "latest" for unversioned installs
        curl --fail --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
          --upload-file "$FILE" \
          "${PACKAGE_REGISTRY_URL}/${skill}/latest/${skill}.skill"

        echo "  -> Published ${skill} ${VERSION} + latest"
      done
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG
